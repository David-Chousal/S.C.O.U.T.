# Wiring / Interconnect

`scout-reva-system-interconnect.svg` — system-level interconnect diagram (power nets, signal
nets, component blocks) derived from the Rev A schematic. Opens directly in any browser or
vector editor.

This is a communication/assembly aid, not an authoritative source — the KiCad schematic in
[../schematics/](../schematics/) wins if the two ever disagree.
